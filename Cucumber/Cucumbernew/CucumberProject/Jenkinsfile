pipeline {
    agent any

    stages {
        
        stage('RunTest') {
            steps {
                sh './mvn clean install test -Dcucumber.options="--plugin json:target/cucumber-report.json"'
            }
        }
        
}
}